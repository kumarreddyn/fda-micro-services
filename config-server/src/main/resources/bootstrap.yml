server:
  port: 8888
  
spring:
  application:
    name: config-server
  
  cloud:
    config:
      server:
        git:
          uri: https://gitlab.com/krn-fda/fda-config-properties.git
﻿          search-paths: config/*
